---
description: Web问题排查必须使用Playwright定位
alwaysApply: true
---

# Web问题排查规范

## 核心原则

**禁止猜测，也禁止推脱**。任何用户报告的 Web 相关问题（包括数据缺失、数据错误、显示异常、功能失效等）都必须在 Playwright 中验证后，才能判断问题性质和归属。

## 为什么数据问题也在排查范围内

用户说"数据不对"，可能的原因有两种：

| 问题类型 | 实际情况 |
|---------|---------|
| **数据问题** | 数据库/接口返回的数据本身是错的 |
| **显示问题** | 数据是对的，但前端渲染/展示有误 |

**通过 Playwright 访问页面**，AI 可以直接观察用户看到的结果，从而区分这两种情况：
- 如果页面显示的值和用户报告的不一致 → 显示问题
- 如果页面显示的和数据源查出来的一样 → 数据问题（需要进一步追查数据源）

**结论**：AI 不应该以"这是数据问题"为由拒绝排查，因为排查本身就是用来判断问题类型的过程。

## 具体要求

### 1. 所有 Web 问题都先用 Playwright

当用户报告以下任何情况时：
- 数据不对、缺失、错误
- 页面显示异常
- 功能不工作
- 布局乱了

**第一步永远是**：使用 Playwright 访问目标页面，获取实际证据。

### 2. 禁止行为

- 禁止在未访问页面的情况下声称"这是数据/后端的问题"
- 禁止在未访问页面的情况下声称"这不在规则范围内"
- 禁止在未检查控制台的情况下声称"没有错误"
- 禁止在未查看元素的情况下声称"元素存在/不存在"

### 3. Playwright 使用流程

1. **访问页面**：使用 `playwright` 或 `pyppeteer` 打开目标 URL
2. **获取证据**：截图、保存 HTML、捕获控制台日志
3. **定位元素**：检查 DOM 结构、CSS 样式、实际显示的内容
4. **对比分析**：
   - 页面显示的内容是什么？
   - 与用户报告的问题是否吻合？
   - 与数据源的实际数据是否一致？
5. **判断归属**：如果是数据问题，继续排查数据源；如果是显示问题，修复前端代码

### 4. 示例

```python
# ❌ 禁止：推脱问题
# "这是数据问题，应该由后端处理"
# "这个问题不在我的规则范围内"

# ✅ 正确：先访问页面再判断
from playwright.sync_api import sync_playwright

with sync_playwright() as p:
    browser = p.chromium.launch()
    page = browser.new_page()
    page.goto("https://example.com")
    page.screenshot(path="debug.png")
    
    # 获取页面实际显示的内容
    displayed_data = page.inner_text(".data-container")
    
    # 对比：用户报告的数据 vs 实际显示 vs 数据源数据
    # 然后才能判断问题归属
```

### 5. 输出要求

排查 Web 问题时，必须提供：

1. **实际截图**：证明问题的真实存在
2. **控制台日志**：捕获的错误信息（如果有）
3. **元素检查结果**：DOM 结构或 CSS 样式的实际值
4. **问题分析**：
   - 页面实际显示了什么
   - 与预期/报告的差异是什么
   - 问题归属于：□ 前端显示问题 □ 数据源问题
5. **处理建议**：根据归属给出下一步行动

### 6. 善后清理

问题解决后，**必须**清理排查过程中生成的临时文件：

- 截图文件（如 `debug.png`、`screenshot_*.png`）
- 临时 HTML 文件（如 `debug_*.html`）
- 导出/保存的页面文件（如 `*.html` 保存的页面）
- 调试用的日志文件（如 `console_*.log`、`debug_*.json`）
- 其他用于排查的临时文件

**清理时机**：在确认问题已解决、修复方案已验证后，立即执行清理。不得将调试文件留在代码仓库中。

